{% comment %}theme-check-disable UndefinedObject{% endcomment %}
{{ 'section-footer.css' | asset_url | stylesheet_tag }}
{{ 'component-newsletter.css' | asset_url | stylesheet_tag }}
{{ 'component-list-menu.css' | asset_url | stylesheet_tag }}
{{ 'component-list-payment.css' | asset_url | stylesheet_tag }}
{{ 'component-list-social.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .footer {
		--logo-width: {{ settings.logo_width }}px;
    margin-top: {{ section.settings.margin_top | times: 0.75 | round: 0 }}px;
		padding-top: {{ section.settings.padding_top_mb }}px;
  }
	
	.footer__content-bottom {
		padding-bottom: {{ section.settings.padding_bottom_mb }}px;
	}

  .footer__bottom-image {
      margin-top: {{ section.settings.image_margin_top_mb }}px;
      margin-bottom: {{ section.settings.image_margin_bottom_mb }}px;
    }

  @media screen and (min-width: 750px) {
		.footer__content-bottom {
			padding-bottom: {{ section.settings.padding_bottom }}px;
		}
		.footer {
			margin-top: {{ section.settings.margin_top }}px;
			padding-top: {{ section.settings.padding_top }}px;
    }

    .footer__bottom-image {
      margin-top: {{ section.settings.image_margin_top }}px;
      margin-bottom: {{ section.settings.image_margin_bottom }}px;
    }
    
  }

	@media only screen and (min-width: 1560px) {
		.extra-large-layout .footer {
			margin-top: calc({{ section.settings.margin_top }}px * 1.333333);
			padding-top: calc({{ section.settings.padding_top }}px * 1.333333);
    }
  }

  .footer__column--contact {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    align-items: flex-start;
  }
  .footer__contact-row {
    display: flex;
    align-items: center;
    gap: 1rem;
  }
  .footer__column--contact .footer__list-social {
    margin: 0;
  }
  .footer__column--contact .list-social__link {
    padding: 0;
  }
  .footer__column--contact .list-social__item {
    --icon-size: 1.8rem;
  }

  /* Brand block (left) + menu columns layout for the footer top */
  @media screen and (min-width: 990px) {
    .footer__content-top .footer__blocks-wrapper.grid {
      display: flex;
      flex-wrap: wrap;
      align-items: flex-start;
      justify-content: space-between;
      gap: 3rem 1.5rem;
      grid-template-columns: none;
    }
    .footer__blocks-wrapper .footer-block:not(.footer-block--menu) {
      flex: 0 0 22rem;
    }
    .footer__blocks-wrapper .footer-block--menu {
      flex: 0 1 auto;
      max-width: 13rem;
    }
  }
  .footer-block__brand-description,
  .footer__menu-link .link,
  .footer__contact-email,
  .footer__column--contact span,
  .footer-block__newsletter-text {
    font-size: 1.4rem;
  }
  .footer-block__brand-description {
    margin-top: 0.8rem;
    max-width: 30rem;
  }

  /* Brand block: separate it from the menu columns */
  @media screen and (min-width: 990px) {
    .footer__blocks-wrapper .footer-block:not(.footer-block--menu) {
      margin-right: 3rem;
    }
  }
  /* Visible (light) social icons in the footer */
  .footer .footer__list-social .list-social__link {
    color: rgb(var(--color-foreground));
  }
  /* Brand block inner spacing + left alignment */
  .footer-block__brand-info .footer__list-social {
    margin-top: 1.6rem;
    justify-content: flex-start;
  }
  .footer-block__brand-info .list-social__link {
    padding: 1.1rem;
    height: 4.4rem;
  }
  .footer-block__brand-info .list-social__item {
    --_opacity: 0;
  }
  .footer-block__brand-info .list-social__link[aria-disabled='true'] {
    opacity: 0.35;
    pointer-events: none;
    cursor: default;
  }
  .footer-block__brand-localization {
    margin-top: 1.6rem;
    font-size: 1.4rem;
  }
  .footer-block__brand-localization .localization-form__select,
  .footer-block__brand-localization .button {
    font-size: 1.4rem;
  }

  .footer__column--copyright {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .footer__copyright-text {
    font-size: 1.4rem;
  }
  .footer__content-bottom .footer__localization .localization-form__select,
  .footer__content-bottom .footer__localization .button {
    font-size: 1.4rem;
  }

  /* Hide the dark 'Powered by Shopify' policy bar */
  .footer-policy-section {
    display: none;
  }

  /* extra breathing room at the very bottom */
  .footer {
    padding-bottom: 30px;
  }

  /* Footer country/currency dropdown -> clean compact blue card (like the sort select) */
  .footer .disclosure__list-wrapper {
    background-color: rgb(var(--color-background));
    border: none;
    border-radius: 0.8rem;
    box-shadow: 0 0.6rem 2rem rgba(0, 0, 0, 0.25);
  }
  .footer .disclosure__list {
    padding-top: 0.4rem;
    padding-bottom: 0.4rem;
  }
  .footer .disclosure__link {
    line-height: 1.3;
    padding: 0.7rem 1.4rem;
    color: rgb(var(--color-foreground));
  }
  .footer .disclosure__link .localization-form__currency {
    opacity: 0.6;
  }

  /* Selected country: bold + dot, no underline, no hover block */
  .footer .disclosure__list .disclosure__link[aria-current='true'],
  .footer .disclosure__list .disclosure__link[aria-current='true'] .localization-form__currency {
    text-decoration: none;
  }
  .footer .disclosure__list .disclosure__link[aria-current='true'] {
    font-weight: 700;
    color: rgb(var(--color-foreground));
    position: relative;
  }
  .footer .disclosure__list .disclosure__link[aria-current='true']::after {
    content: '\2022';
    position: absolute;
    right: 1.4rem;
    top: 50%;
    transform: translateY(-50%);
  }
{%- endstyle -%}

<footer class="footer color-{{ section.settings.color_scheme }} gradient">

    {%- if section.settings.image != blank and section.settings.image_position_mb == 'top' -%}
      <div class="footer__bottom-image footer__bottom-image--top {% if section.settings.image_full_width == false %} page-width {% endif %} medium-hide large-up-hide">
        {%- if section.settings.image_mobile != blank and section.settings.image_position_mb == 'top' -%}
          <div
            class="
              media media--{{  section.settings.image_height }}
              {% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}
            "
            {% if section.settings.image_height == 'adapt-image' %}
              style="--image-height: {{ 1 | divided_by: section.settings.image_mobile.aspect_ratio | times: 100 }}%;"
            {% endif %}
          >
            {%- liquid
              assign widths = '375, 550, 750, 1100, 1500, 1780, 2000, 3000, 3840'
                assign image_height = section.settings.image_mobile.width | divided_by: section.settings.image_mobile.aspect_ratio
                assign sizes = '100vw'
            -%}
            {{
              section.settings.image_mobile
              | image_url: width: 3840
              | image_tag: width: section.settings.image_mobile.width, height: image_height, sizes: sizes, widths: widths, alt: section.settings.image_mobile.alt
            }}
          </div>
        {%- else -%}
          <div
            class="
              media media--{{  section.settings.image_height }}
              {% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}
            "
            {% if section.settings.image_height == 'adapt-image' %}
              style="--image-height: {{ 1 | divided_by: section.settings.image.aspect_ratio | times: 100 }}%;"
            {% endif %}
          >
            {%- liquid
              assign widths = '375, 550, 750, 1100, 1500, 1780, 2000, 3000, 3840'
              assign image_height = section.settings.image.width | divided_by: section.settings.image.aspect_ratio
              assign sizes = '100vw'
            -%}
            {{
              section.settings.image
              | image_url: width: 3840
              | image_tag: width: section.settings.image.width, height: image_height, sizes: sizes, widths: widths, alt: section.settings.image.alt
            }}
          </div>
        {%- endif -%}
      </div>
    {%- endif -%}
  {%- liquid
    assign has_social_icons = true
    if settings.social_facebook_link == blank and settings.social_linkedin_link == blank and settings.social_instagram_link == blank and settings.social_youtube_link == blank and settings.social_tiktok_link == blank and settings.social_twitter_link == blank and settings.social_pinterest_link == blank and settings.social_snapchat_link == blank and settings.social_tumblr_link == blank and settings.social_vimeo_link == blank
      assign has_social_icons = false
    endif

    if settings.brand_image == blank and settings.brand_headline == blank and settings.brand_description == blank
      assign brand_empty = true
    endif

    if section.blocks.size == 1 and section.blocks[0].type == 'brand_information' and brand_empty and has_social_icons == false and section.settings.newsletter_enable == false and section.settings.enable_follow_on_shop == false
      assign only_empty_brand = true
    endif
  -%}
  {%- if section.blocks.size > 0
    or section.settings.newsletter_enable
    or section.settings.show_social
    and has_social_icons == true
    or section.settings.enable_follow_on_shop
  -%}
    {%- unless only_empty_brand -%}
      <div class="footer__content-top page-width">
        {%- if section.blocks.size > 0 -%}
          {%- liquid
            if section.blocks.size == 9
              assign footer_grid_class = 'grid--3-col-tablet'
            elsif section.blocks.size > 6
              assign footer_grid_class = 'grid--4-col-desktop'
            elsif section.blocks.size > 4
              assign footer_grid_class = 'grid--3-col-tablet'
            endif
          -%}
          <div
            class="footer__blocks-wrapper grid {% if section.settings.menu_layout != 'accordion' %} grid--2-col {% else %} grid--1-col {% endif %} grid--4-col-tablet {{ footer_grid_class }}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
            {% if settings.animations_reveal_on_scroll %}
              data-cascade
            {% endif %}
          >
            {%- for block in section.blocks -%}
              <{% if block.type == 'link_list' and section.settings.menu_layout == 'accordion' %}accordion-panel{% else %}div{% endif %}
                class="footer-block grid__item{% if block.type == 'link_list' %} footer-block--menu footer-block--menu-{{ section.settings.menu_layout }}{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
                {{ block.shopify_attributes }}
                {% if settings.animations_reveal_on_scroll %}
                  data-cascade
                  style="--animation-order: {{ forloop.index }};"
                {% endif %}
              >
                {%- if block.settings.heading != blank -%}
                  <h2 class="footer-block__heading clr-text-{{ section.settings.top_text_color }} inline-richtext h6 {{ section.settings.heading_weight }}" {%  if section.settings.menu_layout == 'accordion' %}data-component="toggle"{% endif %}>
                    {{- block.settings.heading -}}
										{%- if section.settings.menu_layout == 'accordion' and block.type == 'link_list' -%}
											{% render 'icon-caret', class: 'medium-hide large-up-hide' %}
										{%- endif -%}
                  </h2>
                {%- endif -%}

                {%- case block.type -%}
                  {%- when '@app' -%}
                    {% render block %}
                  {%- when 'text' -%}
                    <div class="footer-block__details-content rte">
                      {{ block.settings.subtext }}
                    </div>
                  {%- when 'link_list' -%}
                    {%- if block.settings.menu != blank -%}
                      <div class="footer-block__details-content footer__menu list-unstyled footer__menu--{{ section.settings.menu_layout }}" {% if section.settings.menu_layout == 'accordion' %} data-component="dropdown" {% endif %}>
                        {%- for link in block.settings.menu.links -%}
                          <div class="footer__menu-link">
                            <a
                              href="{{ link.url }}"
                              class="link link--text list-menu__item list-menu__item--link{% if link.active %} list-menu__item--active{% endif %}"
                            >
															<span class="link-underline">
																{{ link.title }}
															</span>
                            </a>
                          </div>
                        {%- endfor -%}
                      </div>
                    {%- endif -%}
                  {%- when 'brand_information' -%}
                    <div class="footer-block__brand-info">
                      {%- if settings.brand_image != blank -%}
                        {%- assign brand_image_height = settings.brand_image_width
                          | divided_by: settings.brand_image.aspect_ratio
                        -%}
                        <div
                          class="footer-block__image-wrapper"
                          style="max-width: min(100%, {{ settings.brand_image_width }}px);"
                        >
                          {{
                            settings.brand_image
                            | image_url: width: 1100
                            | image_tag:
                              loading: 'lazy',
                              widths: '50, 100, 150, 200, 300, 400, 550, 800, 1100',
															alt: settings.brand_image.alt,
                              height: brand_image_height,
                              width: settings.brand_image_width
                          }}
                        </div>
                      {%- endif -%}
                      {%- if settings.brand_headline != blank -%}
                        <h2 class="footer-block__heading clr-text-{{ section.settings.top_text_color }} rte h3 {{ section.settings.heading_weight }}">
                          {{ settings.brand_headline }}
                        </h2>
                      {%- endif -%}
                      {%- if settings.brand_description != blank -%}
                        <div class="footer-block__brand-description rte">{{ settings.brand_description }}</div>
												
                      {%- endif -%}
                      {%- if block.settings.show_social and has_social_icons -%}
                        {%- render 'social-icons',
                          class: 'footer__list-social',
													style: 'rounded',
													show_placeholders: true
                        -%}
                      {%- endif -%}
                    </div>
                  {%- when 'image' -%}
                    <div class="footer-block__details-content footer-block-image {{ block.settings.alignment }}">
                      {%- if block.settings.image != blank -%}
                        {%- assign image_size_2x = block.settings.image_width | times: 2 | at_most: 5760 -%}
                        <div
                          class="footer-block__image-wrapper"
                          style="max-width: min(100%, {{ block.settings.image_width }}px);"
                        >
                          <img
                            srcset="{{ block.settings.image | image_url: width: block.settings.image_width }}, {{ block.settings.image | image_url: width: image_size_2x }} 2x"
                            src="{{ block.settings.image | image_url: width: 760 }}"
                            alt="{{ block.settings.image.alt | escape }}"
                            loading="lazy"
                            width="{{ block.settings.image.width }}"
                            height="{{ block.settings.image.height }}"
                          >
                        </div>
                      {%- else -%}
                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg placeholder' }}
                      {%- endif -%}
                    </div>
                {%- endcase -%}
              </{% if block.type == 'link_list' and section.settings.menu_layout == 'accordion' %}accordion-panel{% else %}div{% endif %}>
            {%- endfor -%}
          </div>
        {%- endif -%}

        <div
          class="footer-block--newsletter{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
          {% if settings.animations_reveal_on_scroll %}
            data-cascade
          {% endif %}
        >
          {%- if section.settings.newsletter_enable -%}
            <div class="footer-block__newsletter">
              {%- if section.settings.newsletter_heading != blank -%}
                <h2 class="footer-block__heading clr-text-{{ section.settings.top_text_color }} inline-richtext h6 {{ section.settings.heading_weight }}">
                  {{ section.settings.newsletter_heading }}
                </h2>
                <p class="footer-block__newsletter-text">{{ 'sections.footer.newsletter' | t }}</p>
              {%- endif -%}
              {%- form 'customer', id: 'ContactFooter', class: 'footer__newsletter newsletter-form' -%}
                <input type="hidden" name="contact[tags]" value="newsletter">
                <div class="newsletter-form__field-wrapper">
                  <div class="field">
                    <input
                      id="NewsletterForm--{{ section.id }}"
                      type="email"
                      name="contact[email]"
                      class="field__input"
                      value="{{ form.email }}"
                      aria-required="true"
                      autocorrect="off"
                      autocapitalize="off"
                      autocomplete="email"
                      {% if form.errors %}
                        autofocus
                        aria-invalid="true"
                        aria-describedby="ContactFooter-error"
                      {% elsif form.posted_successfully? %}
                        aria-describedby="ContactFooter-success"
                      {% endif %}
                      placeholder="{{ 'newsletter.label' | t }}"
                      required
                    >
                    <label class="field__label" for="NewsletterForm--{{ section.id }}">
											{{ 'sections.footer.newsletter_label' | t }}
                    </label>
                    <button
                      type="submit"
                      class="newsletter-form__button field__button"
                      name="commit"
                      id="Subscribe"
                      aria-label="{{ 'newsletter.button_label' | t }}"
                    >
                      {% render 'icon-short-arrow' %}
                    </button>
                  </div>
									<small class="newsletter-form__message form__message" id="ContactFooter-error">
										{%- if form.errors -%}
												{%- render 'icon-error' -%}
												{{- form.errors.translated_fields.email | capitalize }}
												{{ form.errors.messages.email -}}
										{%- endif -%}
									</small>
                </div>
                {%- if form.posted_successfully? -%}
                  <h3
                    class="newsletter-form__message newsletter-form__message--success form__message--success form__message"
                    id="ContactFooter-success"
                    tabindex="-1"
                    autofocus
                  >
                    {% render 'icon-success' -%}
										{{ 'sections.footer.newsletter_success' | t }}
                  </h3>
                {%- endif -%}
              {%- endform -%}
            </div>
          {%- endif -%}

          {%- if shop.features.follow_on_shop? and section.settings.enable_follow_on_shop -%}
            <div class="footer__follow-on-shop">
              {% comment %} TODO: enable theme-check once `login_button` is accepted as valid filter {% endcomment %}
              {% # theme-check-disable %}
              {{ shop | login_button: action: 'follow' }}
              {% # theme-check-enable %}
            </div>
          {%- endif -%}
        </div>
      </div>
    {%- endunless -%}
  {%- endif -%}

  <div
    class="footer__content-bottom{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %} {% if section.settings.image != blank %} footer__content-bottom--with-image{% endif %}"
    {% if settings.animations_reveal_on_scroll %}
      data-cascade
    {% endif %}
  >
		{% liquid
			assign footer_content_columns = 0
			assign is_language_localization = false
			assign is_country_localization = false

			if section.settings.enable_language_selector
				if localization.available_languages.size > 1
					assign is_language_localization = true
				endif
			endif

			if section.settings.enable_country_selector
				if localization.available_countries.size > 1
					assign is_country_localization = true
				endif
			endif

			if section.settings.show_social and has_social_icons
				assign footer_content_columns = footer_content_columns | plus: 1
			endif
			if is_language_localization and is_country_localization
				assign footer_content_columns = footer_content_columns | plus: 1
			endif
			if section.settings.payment_enable
				assign footer_content_columns = footer_content_columns | plus: 1
			endif
		%}

    <div class="footer__content-bottom-wrapper page-width footer__content-bottom-wrapper--{{ footer_content_columns }}">
      <div class="footer__column footer__localization isolate clr-text-{{ section.settings.button_text_color }}">
        {%- if section.settings.enable_language_selector and localization.available_languages.size > 1 -%}
          <noscript>
            {%- form 'localization', id: 'FooterLanguageFormNoScript', class: 'localization-form' -%}
              <div class="localization-form__select">
                <h2 class="visually-hidden" id="FooterLanguageLabelNoScript">
                  {{ 'localization.language_label' | t }}
                </h2>
                <select
                  class="localization-selector link"
                  name="locale_code"
                  aria-labelledby="FooterLanguageLabelNoScript"
                >
                  {%- for language in localization.available_languages -%}
                    <option
                      value="{{ language.iso_code }}"
                      lang="{{ language.iso_code }}"
                      {%- if language.iso_code == localization.language.iso_code %}
                        selected
                      {% endif %}
                    >
                      {{ language.endonym_name | capitalize }}
                    </option>
                  {%- endfor -%}
                </select>
                {% render 'icon-caret' %}
              </div>
              <button class="button button--tertiary">{{ 'localization.update_language' | t }}</button>
            {%- endform -%}
          </noscript>

          <localization-form>
            {%- form 'localization', id: 'FooterLanguageForm', class: 'localization-form' -%}
              <div class="no-js-hidden">
                <h2 class="caption-large text-body hidden" id="FooterLanguageLabel">
                  {{ 'localization.language_label' | t }}
                </h2>
                {%- render 'language-localization', localPosition: 'FooterLanguage', text_weight: section.settings.localization_text_weight -%}
              </div>
            {%- endform -%}
          </localization-form>
        {%- endif -%}
        {%- if section.settings.enable_country_selector and localization.available_countries.size > 1 -%}
          <noscript>
            {%- form 'localization', id: 'FooterCountryFormNoScript', class: 'localization-form' -%}
              <div class="localization-form__select">
                <h2 class="visually-hidden" id="FooterCountryLabelNoScript">{{ 'localization.country_label' | t }}</h2>
                <select
                  class="localization-selector link"
                  name="country_code"
                  aria-labelledby="FooterCountryLabelNoScript"
                >
                  {%- for country in localization.available_countries -%}
                    <option
                      value="{{ country.iso_code }}"
                      {%- if country.iso_code == localization.country.iso_code %}
                        selected
                      {% endif %}
                    >
                      {{ country.name }} ({{ country.currency.iso_code }}
                      {{ country.currency.symbol }})
                    </option>
                  {%- endfor -%}
                </select>
                {% render 'icon-caret' %}
              </div>
              <button class="button button--tertiary">{{ 'localization.update_country' | t }}</button>
            {%- endform -%}
          </noscript>
          <localization-form>
            {%- form 'localization', id: 'FooterCountryForm', class: 'localization-form' -%}
              <div class="no-js-hidden">
                <h2 class="caption-large text-body hidden" id="FooterCountryLabel">
                  {{ 'localization.country_label' | t }}
                </h2>
                {%- render 'country-localization', localPosition: 'FooterCountry', text_weight: section.settings.localization_text_weight -%}
              </div>
            {%- endform -%}
          </localization-form>
        {%- endif -%}
      </div>
      <div class="footer__column footer__column--copyright clr-text-{{ section.settings.button_text_color }}">
        <small class="footer__copyright-text">© {{ 'now' | date: '%Y' }} {{ shop.name }}</small>
      </div>
      <div class="footer__column footer__column--info">
        {%- if section.settings.payment_enable -%}
          <div class="footer__payment">
            <span class="visually-hidden">{{ 'sections.footer.payment' | t }}</span>
            <ul class="list list-payment" role="list">
              {%- for type in shop.enabled_payment_types -%}
                <li class="list-payment__item">
                  {{ type | payment_type_svg_tag: class: 'icon icon--full-color' }}
                </li>
              {%- endfor -%}
            </ul>
          </div>
        {%- endif -%}
      </div>
    </div>

    {%- if section.settings.image != blank -%}
      <div class="footer__bottom-image {% if section.settings.image_full_width == false %} page-width {% endif %} {% if section.settings.image_position_mb == 'top' %} small-hide {% endif %}">
        <div
          class="
            media media--{{  section.settings.image_height }}
            {% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}
          "
          {% if section.settings.image_height == 'adapt-image' %}
            style="--image-height: {{ 1 | divided_by: section.settings.image.aspect_ratio | times: 100 }}%;"
          {% endif %}
        >
          {%- liquid
            assign widths = '375, 550, 750, 1100, 1500, 1780, 2000, 3000, 3840'
            assign image_height = section.settings.image.width | divided_by: section.settings.image.aspect_ratio
            assign sizes = '100vw'
          -%}
          {{
            section.settings.image
            | image_url: width: 3840
            | image_tag: width: section.settings.image.width, height: image_height, sizes: sizes, widths: widths, alt: section.settings.image.alt
          }}
        </div>
      </div>
    {%- endif -%}
  </div>
	<div class="footer-policy-section color-{{ section.settings.copyright_color_scheme }} gradient {% if section.settings.enable_country_selector == false and section.settings.enable_language_selector == false %} footer__content-bottom-wrapper--center{% endif %}">
		<div class="page-width footer__policy">
				{%- if section.settings.show_policy -%}
					<ul class="footer__policy-menu list-unstyled">
						{%- for policy in shop.policies -%}
							{%- if policy != blank -%}
								<li>
									<a href="{{ policy.url }}" class="link link--text list-menu__item list-menu__item--link{% if link.active %} list-menu__item--active{% endif %}">{{ policy.title }}</a>
								</li>
							{%- endif -%}
						{%- endfor -%}
					</ul>
				{%- endif -%}
			<div class="footer__copyright caption">
				<small class="copyright__content">{{ powered_by_link }}</small>
			</div>
		</div>
	</div>
</footer>

{%- if section.settings.menu_layout == 'accordion' -%}
	<script src="{{ 'footer.js' | asset_url }}" type="module" defer="defer"></script>
{%- endif -%}

{% schema %}
{
  "name": "t:sections.footer.name",
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "link_list",
      "name": "t:sections.footer.blocks.link_list.name",
      "settings": [
        {
          "type": "inline_richtext",
          "id": "heading",
          "default": "Quick links",
          "label": "t:sections.footer.blocks.link_list.settings.heading.label"
        },
        {
          "type": "link_list",
          "id": "menu",
          "default": "footer",
          "label": "t:sections.footer.blocks.link_list.settings.menu.label",
          "info": "t:sections.footer.blocks.link_list.settings.menu.info"
        }
      ]
    },
    {
      "type": "brand_information",
      "name": "t:sections.footer.blocks.brand_information.name",
      "settings": [
        {
          "type": "paragraph",
          "content": "t:sections.footer.blocks.brand_information.settings.paragraph.content"
        },
        {
          "type": "header",
          "content": "t:sections.footer.blocks.brand_information.settings.header__1.content"
        },
        {
          "type": "checkbox",
          "id": "show_social",
          "default": true,
          "label": "t:sections.footer.blocks.brand_information.settings.show_social.label",
          "info": "t:sections.footer.blocks.brand_information.settings.show_social.info"
        }
      ]
    },
    {
      "type": "text",
      "name": "t:sections.footer.blocks.text.name",
      "settings": [
        {
          "type": "inline_richtext",
          "id": "heading",
          "default": "Heading",
          "label": "t:sections.footer.blocks.text.settings.heading.label"
        },
        {
          "type": "richtext",
          "id": "subtext",
          "default": "<p>Share contact information, store details, and brand content with your customers.</p>",
          "label": "t:sections.footer.blocks.text.settings.subtext.label"
        }
      ]
    },
    {
      "type": "image",
      "name": "Image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "range",
          "id": "image_width",
          "min": 50,
          "max": 550,
          "step": 5,
          "unit": "px",
          "label": "Image width",
          "default": 100
        },
        {
          "type": "select",
          "id": "alignment",
          "label": "Image alignment on large screen",
          "options": [
            {
              "value": "",
              "label": "Left"
            },
            {
              "value": "center",
              "label": "Center"
            },
            {
              "value": "right",
              "label": "Right"
            }
          ],
          "default": "center"
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Menu settings"
    },
		{
			"type": "select",
			"id": "menu_layout",
			"label": "Mobile layout",
			"options": [
				{
				"value": "default",
				"label": "Default"
				},
				{
				"value": "accordion",
				"label": "Accordion"
				}
			],
			"default": "accordion"
		},
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "select",
      "id": "heading_weight",
      "options": [
        {
          "value": "fw-400",
          "label": "t:settings_schema.global.settings.text_weight.options__1.label"
        },
        {
          "value": "fw-500",
          "label": "t:settings_schema.global.settings.text_weight.options__2.label"
        },
        {
          "value": "fw-600",
          "label": "t:settings_schema.global.settings.text_weight.options__3.label"
        },
        {
          "value": "fw-700",
          "label": "t:settings_schema.global.settings.text_weight.options__4.label"
        }
      ],
      "default": "fw-500",
      "label": "Heading weight"
    },
		{
      "type": "select",
      "id": "localization_text_weight",
      "options": [
        {
          "value": "fw-400",
          "label": "t:settings_schema.global.settings.text_weight.options__1.label"
        },
        {
          "value": "fw-500",
          "label": "t:settings_schema.global.settings.text_weight.options__2.label"
        },
        {
          "value": "fw-600",
          "label": "t:settings_schema.global.settings.text_weight.options__3.label"
        },
        {
          "value": "fw-700",
          "label": "t:settings_schema.global.settings.text_weight.options__4.label"
        }
      ],
      "default": "fw-400",
      "label": "Localization text weight"
    },
    {
      "type": "header",
      "content": "t:sections.footer.settings.header__1.content",
      "info": "t:sections.footer.settings.header__1.info"
    },
    {
      "type": "checkbox",
      "id": "newsletter_enable",
      "default": true,
      "label": "t:sections.footer.settings.newsletter_enable.label"
    },
    {
      "type": "inline_richtext",
      "id": "newsletter_heading",
      "default": "Subscribe to our emails",
      "label": "t:sections.footer.settings.newsletter_heading.label"
    },
    {
      "type": "header",
      "content": "t:sections.footer.settings.header__9.content",
      "info": "t:sections.footer.settings.header__9.info"
    },
    {
      "type": "checkbox",
      "id": "enable_follow_on_shop",
      "default": true,
      "label": "t:sections.footer.settings.enable_follow_on_shop.label"
    },
    {
      "type": "header",
      "content": "t:sections.footer.settings.header__2.content",
      "info": "t:sections.footer.settings.header__2.info"
    },
    {
      "type": "checkbox",
      "id": "show_social",
      "default": true,
      "label": "t:sections.footer.settings.show_social.label"
    },
    {
      "type": "header",
      "content": "t:sections.footer.settings.header__3.content",
      "info": "t:sections.footer.settings.header__4.info"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "default": true,
      "label": "t:sections.footer.settings.enable_country_selector.label"
    },
    {
      "type": "header",
      "content": "t:sections.footer.settings.header__5.content",
      "info": "t:sections.footer.settings.header__6.info"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "default": true,
      "label": "t:sections.footer.settings.enable_language_selector.label"
    },
    {
      "type": "header",
      "content": "t:sections.footer.settings.header__7.content"
    },
    {
      "type": "checkbox",
      "id": "payment_enable",
      "default": true,
      "label": "t:sections.footer.settings.payment_enable.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.spacing"
    },
    {
      "type": "range",
      "id": "margin_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.footer.settings.margin_top.label",
      "default": 0
    },
		{
      "type": "header",
      "content": "Content color"
    },
		{
			"type": "select",
			"id": "top_text_color",
			"label": "Top heading color",
			"options": [
				{
					"value": "foreground",
					"label": "Text"
				},
				{
					"value": "foreground-secondary",
					"label": "Text secondary"
				},
				{
				"value": "heading",
				"label": "Heading"
				}
			],
			"default": "foreground"
		},
		{
			"type": "select",
			"id": "button_text_color",
			"label": "Bottom content color",
			"options": [
				{
					"value": "foreground",
					"label": "Text"
				},
				{
					"value": "foreground-secondary",
					"label": "Text secondary"
				},
				{
				"value": "heading",
				"label": "Heading"
				}
			],
			"default": "foreground"
		},
    {
      "type": "header",
      "content": "Image banner"
    },
		{
			"type": "checkbox",
			"id": "image_full_width",
			"label": "Full width",
			"default": false,
      "visible_if": "{{ section.settings.image != blank }}"
		},
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "image_picker",
      "id": "image_mobile",
      "label": "Image mobile top",
      "info": "If image mobile is not selected, the regular image will be displayed on mobile devices.",
      "visible_if": "{{ section.settings.image_position_mb == 'top' }}"
    },
    {
      "type": "select",
      "id": "image_height",
      "visible_if": "{{ section.settings.image != blank }}",
      "options": [
        {
          "value": "adapt-image",
          "label": "Adapt to image"
        },
        {
          "value": "cropped",
          "label": "Cropped"
        }
      ],
      "default": "adapt-image",
      "label": "Banner height"
    },
    {
      "type": "range",
      "id": "image_margin_top",
      "visible_if": "{{ section.settings.image != blank }}",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Image margin top",
      "default": 40,
      "info": "Only for desktop"
    },
    {
      "type": "range",
      "id": "image_margin_bottom",
      "visible_if": "{{ section.settings.image != blank }}",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Image margin bottom",
      "default": 0,
      "info": "Only for desktop"
    },
    {
      "type": "range",
      "id": "image_margin_top_mb",
      "visible_if": "{{ section.settings.image != blank }}",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Image margin top mobile",
      "default": 40
    },
    {
      "type": "range",
      "id": "image_margin_bottom_mb",
      "visible_if": "{{ section.settings.image != blank }}",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Image margin bottom mobile",
      "default": 40
    },
    {
      "type": "select",
      "id": "image_position_mb",
      "visible_if": "{{ section.settings.image != blank }}",
      "options": [
        {
          "value": "default",
          "label": "Default"
        },
        {
          "value": "top",
          "label": "Top"
        }
      ],
      "default": "top",
      "label": "Banner position mobile"
    },
    {
      "type": "header",
      "content": "Copyright"
    },
    {
      "type": "checkbox",
      "id": "show_policy",
      "default": false,
      "label": "t:sections.footer.settings.show_policy.label"
    },
		{
      "type": "color_scheme",
      "id": "copyright_color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 200,
      "step": 2,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 200,
      "step": 2,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    },
		{
      "type": "range",
      "id": "padding_top_mb",
      "min": 0,
      "max": 200,
      "step": 2,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top_mb",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom_mb",
      "min": 0,
      "max": 200,
      "step": 2,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom_mb",
      "default": 36
    }
  ],
  "default": {
    "blocks": [
      {
        "type": "link_list"
      },
      {
        "type": "text"
      }
    ]
  }
}
{% endschema %}
